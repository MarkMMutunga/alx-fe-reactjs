# GitHub API Configuration
# GitHub API Key (optional for public API, but recommended for higher rate limits)
# Get your token from: https://github.com/settings/tokens
# Scope required: public_repo (for accessing public repositories)
VITE_APP_GITHUB_API_KEY=your_github_token_here

# GitHub API Base URL (optional - uses default if not specified)
VITE_APP_GITHUB_API_URL=https://api.github.com

# Application Configuration
VITE_APP_NAME=GitHub User Search
VITE_APP_VERSION=1.0.0

# Instructions:
# 1. Copy this file to .env: cp .env.example .env
# 2. Replace 'your_github_token_here' with your actual GitHub token
# 3. The application will work without a token but with lower rate limits (60/hour vs 5000/hour)
